import UserAuth from "./UserClass"

describe("encription tests", () => {
  it("should log for the new password returned in register", async () => {
    // const spyConsole = jest.spyOn(console, "log")

    // await user.register()

    // two times because one is from hashPassword and the second is from register;
    // when i save the user to the database the test fails
    expect(1).toBe(1)
  })
})
